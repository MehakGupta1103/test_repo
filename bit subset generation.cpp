#include<bits/stdc++.h>
using namespace std;
int main(){
    
    vector<vector<int>> srbseets(vector<int>&nums){
       int n = num.size();
       int subset_ct = (1<<n);
       vector<vector<int>> subset_ct; ++mask){
           vector<int> subset;
           for(int(inti = 0; i < n; ++i){
               if((mask&(1<<i)!=0)){
                   subset.push_back(num[i]);
               }
               subsets.puch_back(subset);
           })
       } 
       //O(2^n)
       return subsets;
    }

}
int main(){
    
}